name: Release

# Publish @olimsaidov/icdp to npm when a GitHub Release is published, using npm
# "trusted publishing" (OIDC) — no NPM_TOKEN secret. npm exchanges this
# workflow's short-lived OIDC token for a publish token and attaches build
# provenance automatically.
#
# Configure on npmjs.com → package settings → Trusted Publisher:
#   Publisher: GitHub Actions · Org/user: olimsaidov · Repo: icdp
#   Workflow filename: release.yml
#
# To cut a release: bump "version" in package.json, commit, then publish a
# GitHub Release whose tag matches (e.g. v0.1.1).
on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for npm trusted publishing + provenance
    steps:
      - uses: actions/checkout@v6
      # NB: no `registry-url` — it makes setup-node write an .npmrc with a
      # placeholder NODE_AUTH_TOKEN + always-auth, which forces (failing) token
      # auth and blocks the OIDC exchange. The registry comes from the checked-in
      # .npmrc (@olimsaidov → npmjs) + publishConfig instead.
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Verify release tag matches package.json version
        run: |
          tag="${GITHUB_REF_NAME#v}"
          pkg="$(node -p "require('./package.json').version")"
          if [ "$tag" != "$pkg" ]; then
            echo "::error::Release tag v$tag does not match package.json version $pkg"
            exit 1
          fi
          echo "Releasing v$pkg"
      # Trusted publishing (OIDC) requires npm >= 11.5.1; node 24 bundles an
      # older npm, so upgrade.
      - run: npm install -g npm@latest
      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm run build
      # OIDC trusted publishing: no token/secret; provenance is automatic.
      - run: npm publish
